# Archived artifacts (stabilization R0)

> **DEPRECATED / SUPERSEDED — do not run in production.**  
> Moved 2026-07-14 during cleanup on `feature/refactor` @ `72d9eb6`.  
> Kept for historical ops reference only.  
> Protected live path remains: `scripts/tmp-vps-safe-deploy.sh` (used by `deploy-safe.ps1`).

## Contents

- `scripts/` — Facebook `probe-*`, `_tmp-probe-*`, and one-off `tmp-vps-*` helpers (except safe-deploy)
- `backend/` — unused FastAPI / Sinatra stubs (`app.py`, `app.rb`)
