// Legacy Prisma entry is intentionally disabled for the SQLite-backed local CMS.
// Keep this stub so old imports do not break TypeScript while server.ts uses dbHelper.
export const prisma = {} as any;
