{
  "name": "estoria-lead-collector-extension",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts --bundle --outfile=dist/background.js --format=iife --target=chrome110 && esbuild src/content.ts --bundle --outfile=dist/content.js --format=iife --target=chrome110 && esbuild src/popup.ts --bundle --outfile=dist/popup.js --format=iife --target=chrome110"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "typescript": "~5.8.2"
  }
}
