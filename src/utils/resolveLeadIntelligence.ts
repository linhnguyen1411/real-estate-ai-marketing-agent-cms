/**
 * @deprecated Import from `shared/agent-domain` (or `@/shared/agent-domain`).
 * Compatibility shim — preserves existing FE/BE import paths.
 */
export {
  resolveLeadIntelligence,
  resolveLeadIntelligenceFromSources,
  cleanLeadSummary,
  formatVietnamPhoneDisplay,
  formatResolvedBudget,
  type FindingLike,
  type ResolvedLeadIntelligence,
  type LeadIntelligence,
  type AnalysisStatus,
  type ScoreStatus,
  type PersonType,
} from '../../shared/agent-domain';
